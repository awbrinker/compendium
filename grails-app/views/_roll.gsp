<!DOCTYPE html>

<div class="row">
    <button onclick="sendMessageTo(${source}, ${target})" class="bannerButton">
        <asset:image src="d20.png" style="width: 40px; height: 40px"/>
        <p class="rollText">Roll</p>
    </button>
</div>